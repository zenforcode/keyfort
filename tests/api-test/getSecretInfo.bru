meta {
  name: getSecretInfo
  type: http
  seq: 3
}

get {
  url: http://127.0.0.1:8000/secret/{{id}}/info
  body: none
  auth: none
}

tests {
  const id = bru.getEnvVar("id");
  
  test("getSecretInfo should handle a valid request", () => {
    expect(res.getStatus()).to.equal(200);
    console.log(res.getBody())
  });
}
