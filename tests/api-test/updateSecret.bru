meta {
  name: updateSecret
  type: http
  seq: 4
}

put {
  url: http://127.0.0.1:8000/meta/update/{{id}}
  body: json
  auth: none
}

body:json {
  {
    "secret": "Updated super secret value!!!"
  }
}

tests {
  const id = bru.getEnvVar("id");
  
  test("invalidateSecret should handle a valid request", () => {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("invalidateSecret should remove secret of provided id", () => {
    const data = res.getBody();
  
    expect(data).to.equal("OK");
  });
}
